const game = function() {
  //
  let game = new Phaser.Game(800, 600, Phaser.AUTO, '', { preload, create, update });

  function preload() {
  }

  function create() {
  }

  function update() {
  }

  return {}
}()